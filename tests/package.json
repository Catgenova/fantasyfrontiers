{
  "name": "fantasy-frontiers-tests",
  "private": true,
  "type": "module",
  "description": "CI harness for the browser unit tests (headless Chrome via Puppeteer).",
  "scripts": {
    "test:unit": "node run-unit.mjs"
  },
  "dependencies": {
    "puppeteer": "^23.11.1"
  }
}
